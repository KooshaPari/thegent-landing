version: "3"

tasks:
  default:
    desc: Build the Astro site
    cmds:
      - task: build

  build:
    desc: Build the Astro site
    cmds:
      - bun run build

  test:
    desc: Run a clean site validation build
    cmds:
      - task: clean
      - bun run build

  lint:
    desc: Run Astro diagnostics and type checks
    cmds:
      - bunx astro check

  clean:
    desc: Remove generated build artifacts
    cmds:
      - git clean -fdX -- dist .astro
